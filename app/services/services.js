let participants = [];
export const GetParticipant = (participant) => {
    participants.push(participant);
}

export const MakeGroupe = (participants) => {
    // Here were we make our groups The return will be a table of couples 
}
